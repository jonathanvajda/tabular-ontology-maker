{
  "name": "tabular-ontology-maker",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
